# 修复终端窗口闪现问题

## 🐛 问题描述

在使用快捷键切换输入法时,每次都会短暂弹出一个黑色终端窗口,然后立即关闭。这个问题非常影响用户体验。

## 🔍 问题原因

问题出在 `switchInputIfNeeded()` 函数中调用外部程序 `im-select.exe` 时,没有设置隐藏窗口的标志。

**问题代码:**
```go
func switchInputIfNeeded(imkey string) {
    // 直接调用,会弹出控制台窗口
    err := exec.Command("im-select.exe", imkey).Run()
    // ...
}
```

在 Windows 系统中,使用 `exec.Command()` 执行外部程序时,默认会创建一个可见的控制台窗口。

## ✅ 解决方案

使用 Windows 系统调用设置 `SysProcAttr`,添加 `HideWindow` 和 `CREATE_NO_WINDOW` 标志来隐藏窗口。

**修复后的代码:**
```go
func switchInputIfNeeded(imkey string) {
    cmd := exec.Command("im-select.exe", imkey)
    
    // 隐藏控制台窗口 - 关键修复!
    cmd.SysProcAttr = &syscall.SysProcAttr{
        HideWindow:    true,                // 隐藏窗口
        CreationFlags: 0x08000000,          // CREATE_NO_WINDOW 标志
    }
    
    err := cmd.Run()
    // ...
}
```

## 🔧 技术细节

### Windows 进程创建标志

- **`HideWindow: true`**: 告诉 Windows 不要显示窗口
- **`CreationFlags: 0x08000000`**: 对应 Windows API 的 `CREATE_NO_WINDOW` 标志
  - 这个标志会创建一个没有控制台窗口的进程
  - 即使父进程是控制台应用,子进程也不会继承控制台

### 为什么需要两个标志?

1. `HideWindow: true` - 基础的隐藏窗口设置
2. `CREATE_NO_WINDOW (0x08000000)` - 更彻底的解决方案,确保不创建任何控制台窗口

组合使用这两个标志可以确保在各种情况下都不会显示窗口。

## 📝 修改文件

修改了 `main.go`:

1. **添加导入**:
   ```go
   import (
       "syscall"  // 新增
   )
   ```

2. **修改 `switchInputIfNeeded()` 函数**:
   - 创建 `cmd` 对象而不是直接调用
   - 设置 `SysProcAttr` 隐藏窗口
   - 然后调用 `cmd.Run()`

## 🎯 测试验证

编译并测试:

```bash
# 重新编译
go build -ldflags="-H windowsgui" -o switch-input-method.exe

# 运行测试
./switch-input-method.exe
```

**测试步骤:**
1. 启动程序 (托盘图标出现)
2. 按 `Win+J` 切换到英文
3. 按 `Win+K` 切换到中文
4. **验证**: 切换时不应该再出现任何闪现的窗口

## ✨ 效果

- ✅ 切换输入法时完全静默
- ✅ 没有任何窗口闪现
- ✅ 用户体验大幅提升
- ✅ 程序行为更加专业

## 📚 相关 Windows API 常量

```go
const (
    CREATE_NO_WINDOW            = 0x08000000
    CREATE_NEW_CONSOLE          = 0x00000010
    CREATE_NEW_PROCESS_GROUP    = 0x00000200
    DETACHED_PROCESS            = 0x00000008
)
```

对于隐藏窗口的场景,推荐使用:
- `CREATE_NO_WINDOW` (0x08000000) - 最彻底,不创建控制台
- `HideWindow: true` - 配合使用,确保窗口隐藏

## 🎉 总结

通过添加简单的几行代码,完美解决了终端窗口闪现的问题,让程序真正做到了"静默运行"。这是一个典型的 Windows GUI 应用开发中的最佳实践。

---

**修复日期**: 2025-10-03  
**状态**: ✅ 已修复并测试
