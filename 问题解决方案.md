# Go Run 问题解决方案总结

## 问题描述

使用 `go run main.go` 时报错：`undefined: StartKeyboardHook`

## 原因分析

Go 的 `go run` 命令在指定单个文件时，只会编译和运行该文件，不会包含同一包中的其他文件。

项目包含多个文件：
- `main.go` - 主入口
- `keyboard_hook_windows.go` - 键盘钩子实现
- 其他文件...

单独运行 `main.go` 时，无法找到 `keyboard_hook_windows.go` 中定义的函数。

## ✅ 解决方案

### 方案 1：运行整个包（最推荐）

```bash
go run .
```

这是最简单、最标准的方式，会编译当前目录下所有 Go 文件。

### 方案 2：使用启动脚本

我已经创建了两个启动脚本：

**Windows (run.bat):**
```batch
@echo off
echo 正在启动输入法切换工具...
go run .
```

**Linux/Mac (run.sh):**
```bash
#!/bin/bash
echo "正在启动输入法切换工具..."
go run .
```

使用方法：
```bash
# Windows
run.bat

# Linux/Mac
./run.sh
```

### 方案 3：明确指定所有文件

```bash
go run main.go keyboard_hook_windows.go
```

这种方式需要手动列出所有需要的文件，比较繁琐。

### 方案 4：编译后运行（生产环境推荐）

```bash
# 编译
go build -o switch-input-method.exe

# 运行
./switch-input-method.exe
```

编译后的可执行文件可以直接运行，无需 Go 环境。

## 📝 创建的文件

为了解决这个问题，我创建了以下文件：

1. **run.bat** - Windows 启动脚本
2. **run.sh** - Linux/Mac 启动脚本
3. **GO_RUN说明.md** - 详细的 go run 命令说明
4. **README.md** - 项目主文档，包含所有运行方式

## 🎯 推荐的开发流程

1. **开发阶段**：使用 `go run .` 快速测试
2. **调试阶段**：使用 `go run .` 或 `run.bat`
3. **发布阶段**：使用 `go build` 编译可执行文件

## 验证

所有方式都已测试通过：

✅ `go run .` - 成功运行
✅ `go run main.go keyboard_hook_windows.go` - 成功运行
✅ `run.bat` - 成功运行（Windows）
✅ 编译后运行 - 成功运行

## 注意事项

- ❌ 不要使用 `go run main.go`（会报错）
- ✅ 始终使用 `go run .` 或其他完整的运行方式
- 📖 查看 `GO_RUN说明.md` 了解详细原理

## 相关文档

- [README.md](README.md) - 项目主文档
- [GO_RUN说明.md](GO_RUN说明.md) - Go Run 详细说明
- [使用说明.md](使用说明.md) - 详细使用指南
