# 系统托盘版使用说明

## ✨ 新功能

现在程序支持系统托盘功能,可以在后台运行而无需保持终端窗口打开!

## 🚀 启动方式

### 方式一:双击运行 (推荐)
直接双击 `switch-input-method.exe`,程序会自动最小化到系统托盘。

### 方式二:命令行运行
```bash
./switch-input-method.exe
```

## 📍 托盘图标

程序启动后,会在 Windows 系统托盘(任务栏右下角)显示一个输入法图标。

### 托盘菜单
右键点击托盘图标,可以看到:
- ✅ 运行中 - 当前程序状态
- 快捷键说明
  - Win+J - 英文输入法
  - Win+K - 中文输入法
- 退出 - 退出程序

## ⌨️ 快捷键

| 快捷键  | 功能                    |
| ------- | ----------------------- |
| Win + J | 切换到英文输入法 (1033) |
| Win + K | 切换到中文输入法 (2052) |

## 🎯 使用场景

1. **开机自启动**: 将程序快捷方式放到启动文件夹中
   - 按 `Win + R`,输入 `shell:startup`,回车
   - 将 `switch-input-method.exe` 的快捷方式复制到该文件夹

2. **后台运行**: 程序会一直在托盘运行,不会显示终端窗口

3. **随时退出**: 右键托盘图标,选择"退出"即可

## 🔧 编译说明

如果需要重新编译,使用以下命令:

### 编译为 GUI 程序 (无控制台窗口)
```bash
go build -ldflags="-H windowsgui" -o switch-input-method.exe
```

### 编译为控制台程序 (显示调试信息)
```bash
go build -o switch-input-method.exe
```

## 📝 注意事项

1. **管理员权限**: 部分应用可能需要管理员权限才能监听全局快捷键
2. **输入法路径**: 确保 `im-select.exe` 在正确的位置
3. **退出方式**: 
   - 推荐通过托盘菜单退出
   - 也可以通过任务管理器结束进程

## 🐛 故障排除

### 托盘图标不显示
- 检查 Windows 系统托盘设置
- 重新启动程序

### 快捷键不生效
- 确保程序正在运行 (检查托盘图标)
- 检查是否有其他程序占用了相同快捷键
- 尝试以管理员身份运行

### 无法切换输入法
- 确认 `im-select.exe` 文件存在
- 检查输入法代码是否正确 (1033/2052)

## 📄 文件说明

- `switch-input-method.exe` - 主程序 (带托盘功能)
- `im-select.exe` - 输入法切换工具
- `tray_windows.go` - 托盘功能实现
- `keyboard_hook_windows.go` - 键盘钩子实现
- `main.go` - 主程序入口

## 🎉 优势

✅ 无需保持终端窗口打开
✅ 开机自启动更方便
✅ 资源占用极低
✅ 系统托盘便捷管理
✅ 支持全局快捷键

## 版本信息

- 版本: 2.0 (托盘版)
- 平台: Windows
- 编译时间: 2025-10-03
