# v2.4 更新简报

## 🐛 修复的严重问题

v2.3 版本通过阻止 Win 键释放事件来防止开始菜单,但这导致:
- ❌ 系统认为 Win 键一直处于按下状态
- ❌ 后续所有按键都被当作 Win+某键的组合
- ❌ 键盘完全失常,无法正常使用

## ✨ v2.4 解决方案

**改用 Escape 键取消开始菜单**

不再阻止 Win 键释放,而是:
1. ✅ Win 键释放事件正常传递 → 系统状态正确
2. ✅ 检测到切换操作时,立即发送 Escape 键
3. ✅ Escape 取消开始菜单的弹出
4. ✅ 键盘功能完全正常

## 📝 技术改动

### 新增 API
- `SendInput` - Windows API 用于模拟键盘输入

### 新增函数
- `sendEscapeKey()` - 发送 Escape 键(按下+释放)

### 核心逻辑修改
```go
// v2.3 - 错误方案
if wasPressed && triggered {
    return 1  // 阻止释放 → 导致 Win 键"卡住"
}

// v2.4 - 正确方案  
if wasPressed && triggered {
    go sendEscapeKey()  // 发送 Escape 取消菜单
}
// Win 键释放正常传递 → 系统状态正确
```

## 🎯 效果

| 功能         | v2.3   | v2.4   |
| ------------ | ------ | ------ |
| 切换输入法   | ✅      | ✅      |
| 阻止开始菜单 | ✅      | ✅      |
| Win键状态    | ❌ 错误 | ✅ 正确 |
| 键盘功能     | ❌ 失常 | ✅ 正常 |

## 📦 使用说明

1. 重新编译:
   ```bash
   go build -ldflags="-H windowsgui" -o switch-input-method.exe
   ```

2. 测试功能:
   - Win+J → 切换英文
   - Win+K → 切换中文
   - 单独按 Win → 开始菜单正常
   - 切换后立即输入 → 键盘正常

## 🔧 技术参考

这是业界标准方案,AutoHotkey、PowerToys 等工具都采用此方法。

---

**版本**: v2.4  
**日期**: 2025年10月3日  
**状态**: ✅ 问题已完全解决
