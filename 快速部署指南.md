# 快速部署指南

## 📦 部署步骤

### 1. 准备文件

创建一个文件夹，将以下文件放入：

```
兴宜街道红旗路输入法切换工具/
├── switch-input-method.exe    ← 主程序（已编译）
└── im-select.exe              ← 输入法切换工具（需下载）
```

### 2. 获取 im-select.exe

**方法1: 从项目仓库下载**
- 访问: https://github.com/daipeihust/im-select/releases
- 下载 Windows 版本
- 解压得到 `im-select.exe`

**方法2: 如果当前目录已有**
- 复制当前目录的 `im-select.exe` 到主程序同目录即可

### 3. 验证部署

检查文件结构：
```
✅ switch-input-method.exe 存在
✅ im-select.exe 存在
✅ 两个文件在同一目录
```

### 4. 首次运行

双击 `switch-input-method.exe`

**成功标志:**
- ✅ 系统托盘出现图标
- ✅ 控制台输出启动信息
- ✅ Win+J/K 可以切换输入法

### 5. 设置开机自启动（可选）

1. 按 `Win + R`
2. 输入 `shell:startup`
3. 复制 `switch-input-method.exe` 的快捷方式到该文件夹

**或者使用脚本创建快捷方式:**

创建 `create-startup.bat`:
```batch
@echo off
set SCRIPT_DIR=%~dp0
set TARGET=%SCRIPT_DIR%switch-input-method.exe
set STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
powershell "$ws = New-Object -ComObject WScript.Shell; $s = $ws.CreateShortcut('%STARTUP%\兴宜街道红旗路输入法切换.lnk'); $s.TargetPath = '%TARGET%'; $s.Save()"
echo 开机自启动设置成功！
pause
```

运行此批处理文件即可自动创建开机启动快捷方式。

## 🎯 推荐部署位置

### 方案1: 用户程序目录（推荐）
```
C:\Users\你的用户名\Programs\IMSwitcher\
├── switch-input-method.exe
└── im-select.exe
```

### 方案2: Program Files
```
C:\Program Files\IMSwitcher\
├── switch-input-method.exe
└── im-select.exe
```
注意: 可能需要管理员权限

### 方案3: 便携式（U盘）
```
E:\PortableApps\IMSwitcher\
├── switch-input-method.exe
└── im-select.exe
```

## ✅ 部署检查清单

- [ ] switch-input-method.exe 已准备
- [ ] im-select.exe 已下载
- [ ] 两个文件在同一目录
- [ ] 双击运行测试成功
- [ ] Win+J 切换到英文成功
- [ ] Win+K 切换到中文成功
- [ ] 托盘图标显示正常
- [ ] 右键菜单功能正常
- [ ] （可选）开机自启动已设置

## 🔧 如果从源码编译

```bash
# 1. 克隆或进入项目目录
cd d:/safe/switch-input-method

# 2. 编译 GUI 版本
go build -ldflags="-H windowsgui" -o switch-input-method.exe

# 3. 确保 im-select.exe 在同目录
# 如果没有，从 https://github.com/daipeihust/im-select 下载

# 4. 测试运行
./switch-input-method.exe
```

## 🎉 完成！

现在你可以：
1. ✅ 使用 Win+J 切换到英文
2. ✅ 使用 Win+K 切换到中文
3. ✅ 右键托盘图标查看菜单
4. ✅ 右键托盘图标退出程序

## 📝 注意事项

### 单例保护
- 程序只能运行一个实例
- 重复启动会显示提示并自动退出
- 检查托盘图标确认程序在运行

### 权限需求
- 一般情况下不需要管理员权限
- 某些应用可能需要管理员权限才能监听快捷键
- 如果快捷键不生效，尝试以管理员身份运行

### 路径要求
- **必须**: `im-select.exe` 与 `switch-input-method.exe` 在同一目录
- 程序会自动查找同目录的 `im-select.exe`
- 无需配置任何路径

---

**部署完成后，享受高效的输入法切换体验！** 🎊
