# v2.5 新特性 - 增强版 im-select

## 🎉 重大突破

v2.5 版本实现了**纯 Go 语言的增强版 im-select**,彻底解决了原版无法区分不同中文输入法的问题!

---

## 核心优势

### 原版 im-select 的局限

```bash
# 所有中文输入法都显示为相同的值
$ im-select.exe
2052  # 微软拼音?搜狗拼音?无法区分!
```

### v2.5 增强版的解决方案

```bash
# 使用完整的 HKL,精确识别每个输入法
$ im-select-enhanced.exe -l
0x08040804  # 中文标准键盘
0xE0200804  # 中文输入法 (布局 0xE020)
0xE00E0804  # 微软拼音新体验
0x04090409  # 英语(美国)
```

---

## 技术原理

Windows 输入法由 **HKL (Keyboard Layout Handle)** 标识,这是一个 32/64 位值:

```
┌─────────────────┬─────────────────┐
│   高 16 位      │   低 16 位      │
│  布局 ID        │  语言 ID        │
│  (Layout ID)    │  (LANGID)       │
│  ↓              │  ↓              │
│  区分输入法     │  区分语言       │
└─────────────────┴─────────────────┘

示例: 0xE00E0804
      │    └─ 0x0804 = 2052 (中文简体)
      └────── 0xE00E = 微软拼音新体验
```

- **原版问题:** 只使用低 16 位(语言 ID) → 无法区分不同中文输入法 ❌
- **v2.5 解决:** 使用完整 32 位 HKL → 精确识别每个输入法 ✅

---

## 新增功能

### 1. 独立命令行工具

```bash
# 查看当前输入法(详细模式)
$ im-select-enhanced.exe -v
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
当前输入法:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HKL:      0xE00E0804
语言ID:   0x0804
布局ID:   0xE00E
描述:     中文(简体) - 微软拼音新体验

# 列出所有输入法
$ im-select-enhanced.exe -l -v

# 切换输入法
$ im-select-enhanced.exe -s 0xE00E0804
```

### 2. 主程序集成

主程序 `switch-input-method.exe` 已集成增强版功能:

- 自动使用新 API(如果失败则降级到原版)
- 托盘菜单中的"快速绑定"功能显示完整 HKL
- 配置文件支持新旧两种格式

### 3. 配置示例

```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "0x04090409",
      "description": "Win+J → 英语"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "0xE00E0804",
      "description": "Win+K → 微软拼音"
    },
    {
      "modifier_key": 91,
      "function_key": 76,
      "im_key": "0x08040804",
      "description": "Win+L → 中文标准键盘"
    }
  ]
}
```

---

## 如何获取输入法的 HKL?

### 方法 1: 使用命令行

```bash
# 切换到你想要的输入法
# 然后运行:
im-select-enhanced.exe -v

# 记下显示的 HKL 值
```

### 方法 2: 列出所有

```bash
# 显示所有已安装的输入法
im-select-enhanced.exe -l -v

# 从输出中找到你需要的
```

### 方法 3: 托盘菜单

1. 切换到目标输入法
2. 右键托盘图标
3. 点击"配置管理" → "快速绑定当前输入法"
4. 查看显示的 HKL

---

## 常用布局 ID

| 布局 ID | 描述           |
| ------- | -------------- |
| 0x0409  | 英语标准键盘   |
| 0x0804  | 中文标准键盘   |
| 0xE00E  | 微软拼音新体验 |
| 0xE010  | 微软五笔新体验 |
| 0xE001  | 微软拼音(旧版) |
| 0xE002  | 微软五笔(旧版) |

**提示:** 第三方输入法(搜狗、RIME等)的布局 ID 在不同系统上可能不同,请使用 `im-select-enhanced -l -v` 查询。

---

## 性能对比

| 项目           | 原版 im-select | v2.5 增强版   |
| -------------- | -------------- | ------------- |
| 进程启动       | 需要           | 不需要        |
| API 调用       | 间接(外部进程) | 直接(syscall) |
| 响应时间       | 20-50ms        | 1-5ms         |
| 依赖文件       | im-select.exe  | 无依赖        |
| 区分中文输入法 | ❌              | ✅             |

---

## 兼容性

- ✅ **向后兼容:** 旧配置文件(使用 `"im_key": "2052"`)仍然可用
- ✅ **自动降级:** 如果新 API 失败,自动回退到原版 im-select.exe
- ✅ **混合使用:** 支持在同一配置文件中混合使用新旧格式

---

## 相关文档

- [增强版im-select快速指南.md](./增强版im-select快速指南.md) - 快速上手
- [IM-SELECT-ENHANCED.md](./IM-SELECT-ENHANCED.md) - 完整技术文档
- [v2.5更新日志-增强版im-select.md](./v2.5更新日志-增强版im-select.md) - 详细更新日志
- [v2.5发布简报.md](./v2.5发布简报.md) - 版本简报

---

## 编译

```bash
# 编译独立的 im-select 工具
go build -ldflags="-s -w" -o im-select-enhanced.exe ./cmd/im-select

# 编译主程序(已集成增强版功能)
go build -ldflags="-s -w -H windowsgui" -o switch-input-method.exe
```

---

## 示例场景

### 场景 1: 同时使用微软拼音和搜狗拼音

```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "0x04090409",
      "description": "Win+J → 英语"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "0xE00E0804",
      "description": "Win+K → 微软拼音"
    },
    {
      "modifier_key": 91,
      "function_key": 76,
      "im_key": "0xE0200804",
      "description": "Win+L → 搜狗拼音"
    }
  ]
}
```

### 场景 2: 多语言工作环境

```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "0x04090409",
      "description": "Win+J → 英语"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "0xE00E0804",
      "description": "Win+K → 中文拼音"
    },
    {
      "modifier_key": 91,
      "function_key": 76,
      "im_key": "0x04110411",
      "description": "Win+L → 日语"
    },
    {
      "modifier_key": 91,
      "function_key": 59,
      "im_key": "0x04120412",
      "description": "Win+; → 韩语"
    }
  ]
}
```

---

## 总结

v2.5 版本通过实现纯 Go 语言的增强版 im-select,使用完整的 HKL 结构,成功解决了原版无法区分不同中文输入法的核心问题!

**主要成就:**
- ✅ 无需外部依赖
- ✅ 性能显著提升
- ✅ 精确识别每个输入法
- ✅ 完全兼容旧版本
- ✅ 详尽的文档和示例

---

**版本:** v2.5.0  
**发布日期:** 2025年10月4日  
**License:** MIT
