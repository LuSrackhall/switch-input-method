# Windows 输入法快速切换工具

## ✨ 功能特性

- 🎯 **全局快捷键**: Win+J / Win+K 快速切换中英文输入法
- 🚫 **阻止系统快捷键**: 完全拦截 Win+J/K，不会触发系统原有功能
- 🔒 **底层钩子**: 使用 Windows Low-Level Keyboard Hook 实现
- 💪 **稳定可靠**: 异步切换，带锁防止并发问题

## 📋 系统要求

- Windows 10/11
- 已安装 `im-select.exe` 工具

## 🚀 快速开始

### 1. 安装 im-select

从 [daipeihust/im-select](https://github.com/daipeihust/im-select) 下载 Windows 版本，并将 `im-select.exe` 放到系统可访问的路径。

### 2. 运行程序

有多种运行方式可选：

#### 方式 1：运行整个包（推荐用于开发）

```bash
go run .
```

#### 方式 2：使用启动脚本

Windows 环境：
```bash
run.bat
```

Linux/Mac 环境：
```bash
./run.sh
```

#### 方式 3：指定所有文件运行

```bash
go run main.go keyboard_hook_windows.go
```

#### 方式 4：编译后运行（推荐用于生产）

```bash
# 编译
go build -o switch-input-method.exe

# 运行
./switch-input-method.exe
```

程序启动后会显示：
```
===========================================
  输入法快速切换工具 - Windows 版本
===========================================
正在安装键盘钩子...
快捷键设置:
  Win+J - 切换到英文输入法 (1033)
  Win+K - 切换到中文输入法 (2052)
按 Ctrl+C 退出程序
----------------------------------------
键盘钩子安装成功！
```

### 3. 使用快捷键

- **Win + J**: 切换到英文输入法
- **Win + K**: 切换到中文输入法

当切换成功时，会在终端显示：
```
✅ 已切换到英文输入法
```

## ⚙️ 自定义配置

### 修改 im-select.exe 路径

编辑 `main.go` 文件，找到 `switchInputIfNeeded` 函数：

```go
func switchInputIfNeeded(imkey string) {
	err := exec.Command("你的路径\\im-select.exe", imkey).Run()
	// ...
}
```

### 修改输入法代码

查看当前输入法代码：
```bash
im-select.exe
```

常见输入法代码：
- `1033` - 英语(美国)
- `2052` - 中文(简体，中国)
- `1028` - 中文(繁体，台湾)

在 `keyboard_hook_windows.go` 中修改：

```go
// 检测 Win+J (切换到英文)
if winKeyPressed && vkCode == VK_J {
    go func() {
        switchMutex.Lock()
        defer switchMutex.Unlock()
        switchInputIfNeeded("1033") // 修改这里
    }()
    return 1
}
```

### 修改快捷键

在 `keyboard_hook_windows.go` 中修改虚拟键码：

```go
const (
    VK_LWIN = 0x5B  // 左 Win 键
    VK_RWIN = 0x5C  // 右 Win 键
    VK_J    = 0x4A  // J 键 - 改为其他键
    VK_K    = 0x4B  // K 键 - 改为其他键
)
```

常用虚拟键码：
- `0x41-0x5A`: A-Z 键
- `0x30-0x39`: 0-9 键（主键盘）
- `0xBA`: 分号键
- `0xBB`: 等号键

完整列表参考: [Virtual-Key Codes](https://learn.microsoft.com/en-us/windows/win32/inputdev/virtual-key-codes)

## 🔧 技术实现

### 核心技术

1. **Low-Level Keyboard Hook (WH_KEYBOARD_LL)**
   - 在键盘消息到达应用程序之前拦截
   - 可以阻止事件继续传递给系统

2. **事件拦截机制**
   ```go
   // 检测到目标快捷键
   if winKeyPressed && vkCode == VK_J {
       // 执行切换操作
       go switchInputIfNeeded("1033")
       // 返回 1 阻止事件传递
       return 1
   }
   // 其他按键返回 CallNextHookEx 继续传递
   ```

3. **异步切换 + 互斥锁**
   ```go
   go func() {
       switchMutex.Lock()
       defer switchMutex.Unlock()
       switchInputIfNeeded("2052")
   }()
   ```

### 与 gohook 的区别

| 特性           | gohook     | Low-Level Hook |
| -------------- | ---------- | -------------- |
| 事件拦截       | ❌ 只能监听 | ✅ 可以拦截     |
| 阻止传递       | ❌ 不支持   | ✅ 支持         |
| 系统快捷键冲突 | ⚠️ 会触发   | ✅ 不会触发     |
| 实现复杂度     | 简单       | 中等           |

## ⚠️ 注意事项

1. **管理员权限**: 建议以管理员身份运行，确保全局钩子生效
2. **杀毒软件**: 可能被标记为可疑程序，请添加信任
3. **资源占用**: 钩子会一直运行，退出程序会自动卸载

## 🐛 故障排除

### 问题：快捷键不生效

**解决方法**:
1. 以管理员权限运行程序
2. 检查终端是否显示 "键盘钩子安装成功"
3. 查看是否有错误提示

### 问题：切换失败

**解决方法**:
1. 确认 `im-select.exe` 路径正确
2. 手动运行 `im-select.exe` 测试
3. 检查输入法代码是否正确

### 问题：系统快捷键仍然触发

**解决方法**:
1. 确认使用的是新编译的版本
2. 检查 `keyboard_hook_windows.go` 是否正确返回 1
3. 重启程序

## 📝 开发

### 编译

```bash
go build -o switch-input-method.exe
```

### 调试

在 `keyboard_hook_windows.go` 中添加调试信息：

```go
fmt.Printf("按键: VK=%d, wParam=%d\n", vkCode, wParam)
```

## 📄 许可证

请参考项目主 LICENSE 文件。
