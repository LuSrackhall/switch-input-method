# v2.4 版本更新总结

## 版本信息
- **版本号:** v2.4.0
- **发布日期:** 2024年10月
- **主要改进:** 用户体验优化和功能增强

---

## 新增功能

### 1. 托盘菜单动态更新 ✨

**功能描述:**
- 配置重新加载后,托盘菜单**自动更新**,无需重启程序
- 菜单项实时显示当前的快捷键绑定
- 提升用户体验,避免频繁重启

**技术实现:**
- 使用全局变量存储菜单项引用
- `updateKeyBindingMenuItems()` 函数动态创建/更新菜单
- `reloadConfigAndUpdateMenu()` 统一处理配置重载和界面更新

**用户操作:**
1. 修改 `config.json` 文件
2. 点击托盘菜单 "重新加载配置"
3. 菜单项和快捷键**立即生效**,无需重启!

---

### 2. 增强的虚拟键码参考 📚

**新增内容:**
- **方向键:** ←↑→↓ (37-40)
- **编辑键:** Page Up/Down, Home, End, Insert, Delete
- **数字小键盘:** Num0-9, 运算符键
- **特殊键:** Backspace, Tab, Enter, Escape, Space
- **符号键:** 分号、逗号、括号等
- **锁定键:** Caps Lock, Num Lock, Scroll Lock

**交互改进:**
- 增加【分类标题】,结构更清晰
- 底部显示 "点击「打开完整参考」查看所有虚拟键码"
- 点击确定按钮后,**自动打开浏览器**访问微软官方文档
- 使用 `ShellExecuteW` API 实现链接跳转

**键码总数:**
- v2.3: ~30 个常用键
- v2.4: **70+ 个键码**,覆盖绝大多数使用场景

---

### 3. 配置重置功能 🔄

**功能描述:**
- 一键恢复默认配置 (Win+J 英文, Win+K 中文)
- 提供二次确认对话框,防止误操作
- 重置后自动重新加载配置

**操作流程:**
1. 点击托盘菜单 "重置为默认配置"
2. 弹出确认对话框,显示默认配置内容
3. 点击"是"确认重置
4. 自动应用新配置,立即生效

**技术细节:**
- `resetConfig()` 函数实现
- 使用 `ShowMessageBoxYesNo()` 显示确认对话框
- 调用 `GetDefaultConfig()` 获取默认配置
- 调用 `reloadConfigAndUpdateMenu()` 应用配置

**安全性:**
- 二次确认机制,避免误操作
- 不会删除原配置文件,只覆盖内容
- 重置失败时显示错误信息

---

### 4. 改进的快速绑定向导 🧭

**旧版本问题:**
- 只显示示例代码
- 需要手动编辑 JSON
- 没有步骤指引

**新版本改进:**

#### 第一步:检测当前输入法
```
━━━━━━━━━━━━━━━━━━━━━━
    快速绑定输入法向导
━━━━━━━━━━━━━━━━━━━━━━

✓ 检测到当前输入法:
  com.apple.keylayout.US

请按照以下步骤操作:
【步骤1】切换到目标输入法
【步骤2】点击「确定」继续
【步骤3】选择快捷键
```

#### 第二步:选择快捷键
```
━━━━━━━━━━━━━━━━━━━━━━
    快速绑定 - 选择快捷键
━━━━━━━━━━━━━━━━━━━━━━

✓ 目标输入法:
  1033 (英文)

请选择快捷键组合:

【推荐组合】
  Win+J / Win+K / Win+L
  Win+I / Win+U / Win+O

【其他组合】
  Ctrl+Alt+字母键
  Win+数字键
```

#### 第三步:配置说明
- 显示完整的 JSON 配置示例
- 自动填充当前检测到的输入法
- 提供按键代码快速参考
- 可选择查看完整虚拟键码表

**用户体验提升:**
- 向导式界面,步骤清晰
- 实时检测输入法,减少手动输入
- 提供推荐快捷键组合
- 配置示例自动生成

---

### 5. 完整的构建文档 📖

**文档内容:**

#### 环境要求
- 操作系统: Windows 10+
- Go 语言: 1.16+
- 必需工具: Git, im-select.exe

#### 依赖安装
- Go 安装详细步骤 (安装包/包管理器)
- 环境变量配置
- Go 模块代理配置 (国内加速)
- 项目依赖下载

#### 构建步骤
- 基础构建: `go build`
- 托盘版构建: `-ldflags="-H windowsgui"`
- 优化构建: `-ldflags="-s -w"`
- 跨版本编译

#### 常见问题 (FAQ)
1. ✅ "go: command not found" - 环境变量未配置
2. ✅ 依赖下载失败 - GOPROXY 设置
3. ✅ "undefined: syscall" - 平台不匹配
4. ✅ 运行时窗口闪现 - 缺少 windowsgui 参数
5. ✅ im-select.exe 找不到 - 路径问题
6. ✅ 钩子注册失败 - 权限或杀毒软件拦截
7. ✅ 编译后体积过大 - 优化参数和 UPX 压缩

#### 开发调试
- 启用调试日志
- 调试构建选项
- Delve 调试器使用
- 依赖管理和更新
- 单元测试
- 性能分析

#### 快速参考
- 快速构建命令表格
- 环境检查清单
- 构建脚本示例 (build.bat)

**文档特点:**
- 📝 详尽的步骤说明
- 🛠️ 实用的命令示例
- ❓ 完整的常见问题解答
- 📊 清晰的表格和代码块
- 🔗 相关资源链接

---

## 技术改进

### 代码结构优化

#### tray_windows.go
```go
// 新增全局变量
var keyBindingMenuItems []*systray.MenuItem

// 新增函数
func updateKeyBindingMenuItems()     // 动态更新菜单项
func reloadConfigAndUpdateMenu()     // 重载配置并更新菜单
func resetConfig()                   // 重置配置
func ShowMessageBoxYesNo()           // 显示是/否对话框
```

#### config_dialog.go
```go
// 改进函数
func ShowKeyCodeReference()          // 增强虚拟键码参考
func ShowQuickBindingMenu()          // 改进快速绑定向导
func getIMDescription()              // 输入法描述生成

// 新增函数
func ShowMessageBoxWithButton()      // 带按钮的消息框
func openURL()                       // 打开浏览器URL
```

### API 调用
- `ShellExecuteW` - 打开浏览器链接
- `MessageBoxW` - 各种样式的消息框
- systray 动态菜单管理

---

## 文件变更

### 修改的文件
- ✅ `tray_windows.go` - 托盘菜单动态更新逻辑
- ✅ `config_dialog.go` - 虚拟键码参考和快速绑定改进
- ✅ `config.go` - 配置管理核心 (已完善)
- ✅ `keyboard_hook_windows.go` - 键盘钩子 (已完善)

### 新增的文件
- ✅ `BUILD.md` - 完整的源码构建文档

---

## 编译优化

### 构建命令对比

**普通构建:**
```bash
go build -o switch-input-method.exe
```
文件大小: ~8-10 MB

**优化构建 (推荐):**
```bash
go build -ldflags="-s -w -H windowsgui" -o switch-input-method.exe
```
文件大小: **~4.5 MB** ✨

**参数说明:**
- `-s` - 去除符号表
- `-w` - 去除 DWARF 调试信息
- `-H windowsgui` - 隐藏控制台窗口

### 体积优化效果
- 减小约 **50-55%** 的文件体积
- 无性能损失
- 启动速度不受影响

---

## 用户体验改进总结

### 之前的问题
1. ❌ 修改配置后需要重启程序
2. ❌ 托盘菜单不更新,容易混淆
3. ❌ 虚拟键码参考不全面
4. ❌ 链接需要手动复制到浏览器
5. ❌ 快速绑定功能简陋
6. ❌ 没有配置重置功能
7. ❌ 缺少构建文档

### 现在的改进
1. ✅ **配置热重载** - 无需重启,菜单自动更新
2. ✅ **实时菜单同步** - 显示内容始终是最新的
3. ✅ **70+ 键码参考** - 涵盖所有常用按键
4. ✅ **一键打开链接** - 点击确定直接跳转浏览器
5. ✅ **向导式绑定** - 三步完成,清晰易懂
6. ✅ **一键重置** - 快速恢复默认配置
7. ✅ **完整文档** - BUILD.md 详尽的构建指南

---

## 测试验证

### 编译测试
```bash
✅ go build -o switch-input-method.exe
✅ go build -ldflags="-s -w -H windowsgui" -o switch-input-method.exe
✅ 无编译错误
✅ 无运行时错误
```

### 功能测试
- ✅ 托盘菜单动态更新正常
- ✅ 虚拟键码参考显示完整
- ✅ 浏览器链接跳转成功
- ✅ 快速绑定向导流程顺畅
- ✅ 配置重置功能正常
- ✅ 快捷键热重载生效

### 文件大小
- 优化前: ~8-10 MB
- 优化后: **4.5 MB** ✨
- 优化率: **~55%**

---

## 向后兼容性

### 配置文件
- ✅ 完全兼容 v2.3 的 `config.json`
- ✅ 无需修改现有配置
- ✅ 自动识别新旧格式

### API 接口
- ✅ 所有公开函数保持兼容
- ✅ 新增函数不影响旧代码
- ✅ 内部重构对外透明

---

## 下一步计划 (可选)

### 可能的后续改进
1. 🔮 可视化配置编辑器 (GUI 表单)
2. 🔮 配置文件版本管理
3. 🔮 快捷键冲突检测
4. 🔮 多语言支持 (i18n)
5. 🔮 主题切换 (暗色/亮色)
6. 🔮 自动更新检查

---

## 致谢

感谢用户提出的宝贵建议和反馈,推动了 v2.4 版本的改进!

---

**版本:** v2.4.0  
**日期:** 2024年10月  
**状态:** ✅ 所有功能已完成并测试通过  
**文件大小:** 4.5 MB (优化构建)
