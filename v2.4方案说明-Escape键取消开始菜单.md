# v2.4 方案说明 - Escape键取消开始菜单

## 问题回顾

### v2.3 的问题
在 v2.3 版本中,我们通过阻止 Win 键释放事件来防止开始菜单弹出:

```go
if wasPressed && triggered {
    return 1  // 阻止 Win 键释放事件
}
```

**这导致了严重问题:**
- 系统永远收不到 Win 键的释放事件
- 系统认为 Win 键一直处于按下状态
- 后续所有按键都被当作 Win+某键的组合键
- 键盘功能完全失常,无法正常使用

## v2.4 解决方案

### 核心思路
**不阻止 Win 键释放,而是发送 Escape 键来取消开始菜单**

这是业界常用的解决方案,原理如下:
1. Win 键释放事件正常传递给系统
2. 系统正确更新 Win 键状态(不再按下)
3. 开始菜单开始准备弹出
4. 立即发送 Escape 键
5. Escape 键取消开始菜单的弹出过程

### 技术实现

#### 1. 新增 Windows API
```go
procSendInput = user32.NewProc("SendInput")
```

#### 2. 新增常量
```go
VK_ESCAPE = 0x1B  // Escape 键
INPUT_KEYBOARD = 1
KEYEVENTF_KEYUP = 0x0002
```

#### 3. 新增数据结构
```go
// 键盘输入结构
type KEYBDINPUT struct {
    WVk         uint16
    WScan       uint16
    DwFlags     uint32
    Time        uint32
    DwExtraInfo uintptr
    Unused      [8]byte
}

// 输入结构
type INPUT struct {
    Type uint32
    Ki   KEYBDINPUT
}
```

#### 4. 实现 sendEscapeKey 函数
```go
func sendEscapeKey() {
    // 按下 Escape
    inputDown := INPUT{
        Type: INPUT_KEYBOARD,
        Ki: KEYBDINPUT{
            WVk:     VK_ESCAPE,
            WScan:   0,
            DwFlags: 0,
            Time:    0,
        },
    }

    // 释放 Escape
    inputUp := INPUT{
        Type: INPUT_KEYBOARD,
        Ki: KEYBDINPUT{
            WVk:     VK_ESCAPE,
            WScan:   0,
            DwFlags: KEYEVENTF_KEYUP,
            Time:    0,
        },
    }

    inputs := []INPUT{inputDown, inputUp}
    procSendInput.Call(
        uintptr(2),
        uintptr(unsafe.Pointer(&inputs[0])),
        uintptr(unsafe.Sizeof(INPUT{})),
    )
}
```

#### 5. 修改 Win 键释放处理逻辑
```go
// 检测 Win 键释放
if wParam == WM_KEYUP || wParam == WM_SYSKEYUP {
    if vkCode == VK_LWIN || vkCode == VK_RWIN {
        wasPressed := winKeyPressed
        triggered := switchTriggered
        winKeyPressed = false

        // 如果触发了切换操作,发送 Escape 取消开始菜单
        if wasPressed && triggered {
            go sendEscapeKey()  // 在 goroutine 中异步发送
        }
        // Win 键释放事件正常传递给系统 (不再 return 1)
    }
}
```

### 关键改进点

#### 1. Win 键状态正确
- Win 键释放事件正常传递
- 系统能正确跟踪 Win 键状态
- 不会导致 Win 键"卡住"的问题

#### 2. 异步发送 Escape
- 使用 `go sendEscapeKey()` 异步发送
- 避免阻塞键盘钩子回调函数
- 提高响应速度

#### 3. 用户体验
- 开始菜单几乎不可见(被 Escape 立即取消)
- 键盘功能完全正常
- Win+J/K 切换输入法流畅

## 工作流程

### 正常 Win 键单击
```
1. 用户按下 Win 键
   → winKeyPressed = true
   → switchTriggered = false

2. 用户释放 Win 键
   → winKeyPressed = false
   → triggered = false
   → 不发送 Escape
   → 开始菜单正常弹出 ✓
```

### Win+J 切换输入法
```
1. 用户按下 Win 键
   → winKeyPressed = true
   → switchTriggered = false

2. 用户按下 J 键
   → switchTriggered = true
   → 切换到英文输入法
   → 阻止 J 键事件传递

3. 用户释放 J 键
   → (无特殊处理)

4. 用户释放 Win 键
   → winKeyPressed = false
   → triggered = true
   → Win 键释放事件正常传递
   → 立即发送 Escape 键
   → 开始菜单被取消 ✓
```

## 优势总结

| 方面       | v2.3 (阻止释放)  | v2.4 (Escape取消) |
| ---------- | ---------------- | ----------------- |
| Win键状态  | ❌ 错误(一直按下) | ✅ 正确            |
| 键盘功能   | ❌ 失常           | ✅ 正常            |
| 开始菜单   | ✅ 不弹出         | ✅ 不弹出          |
| 用户体验   | ❌ 无法使用键盘   | ✅ 完全正常        |
| 技术可靠性 | ❌ 破坏系统状态   | ✅ 符合最佳实践    |

## 测试建议

1. **测试 Win 键单击**
   - 单独按下并释放 Win 键
   - 预期: 开始菜单正常弹出

2. **测试 Win+J 切换**
   - 按下 Win+J
   - 预期: 切换到英文输入法,开始菜单不弹出

3. **测试 Win+K 切换**
   - 按下 Win+K
   - 预期: 切换到中文输入法,开始菜单不弹出

4. **测试后续键盘输入**
   - 切换后立即输入文字
   - 预期: 键盘功能完全正常

5. **测试其他 Win 组合键**
   - Win+E (打开资源管理器)
   - Win+D (显示桌面)
   - 预期: 所有功能正常

## 技术参考

这个方案是业界标准做法,类似工具如:
- AutoHotkey
- PowerToys
- 各类键盘重映射工具

都使用发送 Escape 键的方式来处理开始菜单问题。

## 总结

v2.4 版本通过发送 Escape 键取消开始菜单,完美解决了 v2.3 版本阻止 Win 键释放导致的键盘失常问题,是一个可靠且符合最佳实践的解决方案。
