# v2.4 更新日志 - 可配置按键绑定

## 更新时间
2025年10月3日

## 主要更新

### 🎯 核心功能: 可配置的按键绑定系统

之前版本中,按键组合和输入法绑定都是硬编码的:
- 修饰键固定为 Win 键
- 功能键固定为 J 和 K
- 输入法固定为英文(1033)和中文(2052)

现在全部可由用户自定义配置!

### ✨ 新增功能

#### 1. 配置文件支持 (`config.json`)
- 程序启动时自动加载配置文件
- 如果配置文件不存在,自动创建默认配置
- 支持多个按键绑定
- 可自定义任意修饰键 + 功能键组合
- 可绑定任意输入法

#### 2. 获取当前输入法功能
- 新增 `GetCurrentInputMethod()` 函数
- 调用 `im-select.exe` (不带参数) 获取当前输入法标识符
- 方便用户确定要绑定的输入法 key

#### 3. 动态按键检测
- 重写键盘钩子逻辑,支持根据配置动态响应按键
- 支持任意修饰键(Win/Ctrl/Alt/Shift)
- 支持任意功能键(字母/数字/功能键等)
- 自动维护修饰键状态映射

#### 4. 托盘配置管理菜单
新增配置管理子菜单:
- **查看当前配置**: 显示当前所有按键绑定
- **快速绑定当前输入法**: 显示如何绑定当前输入法的说明
- **虚拟键码参考**: 显示常用按键的虚拟键码对照表
- **打开配置文件**: 直接在记事本中打开配置文件
- **重新加载配置**: 无需重启程序即可应用新配置

#### 5. 动态托盘提示
- 托盘图标的 Tooltip 现在会根据配置动态显示当前的按键绑定
- 托盘菜单中的快捷键说明也会动态显示

### 📝 配置文件格式

```json
{
  "key_bindings": [
    {
      "modifier_key": 91,      // 修饰键虚拟键码 (91=左Win键)
      "function_key": 74,      // 功能键虚拟键码 (74=J键)
      "im_key": "1033",        // 输入法标识符
      "description": "切换到英文输入法"  // 描述
    }
  ]
}
```

### 🔧 技术改进

#### 文件结构
- **config.go**: 配置管理核心逻辑
  - `Config` 和 `KeyBinding` 结构体
  - 配置加载、保存、验证
  - 默认配置生成
  - 按键名称映射

- **config_dialog.go**: 配置界面相关
  - 配置查看对话框
  - 快速绑定向导
  - 虚拟键码参考

- **keyboard_hook_windows.go**: 重构按键钩子
  - 移除硬编码的 `winKeyPressed` 变量
  - 新增 `modifierKeyStates` 映射表
  - 动态按键组合检测
  - 支持多修饰键管理

- **tray_windows.go**: 托盘菜单增强
  - 动态菜单生成
  - 配置管理子菜单
  - 配置重载功能

- **main.go**: 程序初始化
  - 添加配置初始化步骤
  - 改进错误处理

### 📖 新增文档

- **配置指南.md**: 详细的配置说明文档
  - 配置文件格式说明
  - 虚拟键码参考表
  - 配置示例
  - 故障排除

- **config.json.example**: 配置文件示例

### 🎨 用户体验改进

1. **首次运行**: 自动创建默认配置,开箱即用
2. **配置修改**: 支持热重载,无需重启程序
3. **错误提示**: 配置加载失败时使用默认配置并提示
4. **可视化**: 托盘菜单和提示动态显示当前配置
5. **便捷操作**: 一键打开配置文件,一键重载配置

### 🔐 兼容性

- 向后兼容: 如果没有配置文件,自动使用默认配置(Win+J/K)
- 升级平滑: 旧版本用户首次运行会自动创建配置文件

### 💡 使用示例

#### 示例 1: 更改默认快捷键为 Ctrl+1/2
```json
{
  "key_bindings": [
    {
      "modifier_key": 162,
      "function_key": 49,
      "im_key": "1033",
      "description": "切换到英文"
    },
    {
      "modifier_key": 162,
      "function_key": 50,
      "im_key": "2052",
      "description": "切换到中文"
    }
  ]
}
```

#### 示例 2: 添加第三个输入法
```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "1033",
      "description": "切换到英文"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "2052",
      "description": "切换到中文"
    },
    {
      "modifier_key": 91,
      "function_key": 76,
      "im_key": "你的输入法key",
      "description": "切换到第三个输入法"
    }
  ]
}
```

### 🚀 后续计划

1. 图形化配置界面
2. 按键捕获功能(可视化录制按键组合)
3. 配置导入导出
4. 预设配置模板

### 🐛 已知问题

- 配置对话框目前使用简单的消息框,未来会改进为完整的图形界面

### 📋 升级说明

1. 下载新版本程序
2. 首次运行会自动创建 `config.json` 配置文件
3. 可以直接编辑配置文件自定义按键绑定
4. 详细说明请参考 `配置指南.md`

---

## 感谢使用!

如有问题或建议,欢迎反馈。
