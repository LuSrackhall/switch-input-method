# 兴宜街道红旗路输入法切换工具 - 使用指南

## 🎯 v2.2 单文件版 - 最简单的部署方式

### 一句话说明

**只需要一个文件 `switch-input-method.exe`，双击运行即可！**

---

## 📦 快速开始

### 1️⃣ 获取程序

从项目目录获取编译好的文件：
- `switch-input-method.exe` (6.0 MB)

### 2️⃣ 运行程序

**方式一：双击运行**
- 双击 `switch-input-method.exe`
- 系统托盘会出现图标
- 完成！

**方式二：复制到任意位置**
```
可以放在：
- 桌面
- D:\Tools\
- U盘
- 任何你喜欢的地方
```

### 3️⃣ 使用快捷键

| 快捷键      | 功能             |
| ----------- | ---------------- |
| **Win + J** | 切换到英文输入法 |
| **Win + K** | 切换到中文输入法 |

### 4️⃣ 查看菜单

右键点击系统托盘图标：

```
┌────────────────────────────────┐
│ 🏷️ 兴宜街道红旗路输入法切换工具 │
├────────────────────────────────┤
│ ✅ 运行中                      │
├────────────────────────────────┤
│ 快捷键说明                     │
│   Win+J - 英文输入法           │
│   Win+K - 中文输入法           │
├────────────────────────────────┤
│ 退出                           │
└────────────────────────────────┘
```

---

## 🚀 高级使用

### 开机自动启动

**步骤：**

1. 按 `Win + R`，输入：`shell:startup`，回车
2. 创建快捷方式：
   - 右键 `switch-input-method.exe` → 创建快捷方式
   - 将快捷方式拖到启动文件夹
3. 完成！下次开机自动运行

**或使用脚本：**

创建 `add-to-startup.bat` 文件：
```batch
@echo off
set SCRIPT_DIR=%~dp0
set TARGET=%SCRIPT_DIR%switch-input-method.exe
set STARTUP=%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup
powershell "$ws = New-Object -ComObject WScript.Shell; $s = $ws.CreateShortcut('%STARTUP%\兴宜街道红旗路输入法切换.lnk'); $s.TargetPath = '%TARGET%'; $s.Save()"
echo 已添加到开机启动！
pause
```

双击运行即可自动添加到启动项。

---

## ❓ 常见问题

### Q1: 程序已在运行中？

**现象：** 双击程序时弹出提示"程序已在运行中！"

**解决：**
- 这是正常的单例保护
- 检查系统托盘图标（任务栏右下角）
- 如果需要重启，右键托盘图标选择"退出"，然后重新运行

---

### Q2: 如何退出程序？

**方法1：** 右键托盘图标 → 点击"退出"

**方法2：** 任务管理器 → 找到 `switch-input-method.exe` → 结束任务

---

### Q3: 快捷键不生效？

**可能原因：**
1. 程序没有运行（检查托盘图标）
2. 被其他软件占用了快捷键
3. 在某些应用中需要管理员权限

**解决方法：**
- 确认托盘图标存在
- 尝试以管理员身份运行程序
- 检查是否有其他软件占用 Win+J/K

---

### Q4: 找不到托盘图标？

**解决：**
1. 点击任务栏右下角的 `^` 符号（展开隐藏图标）
2. 在展开的菜单中查找
3. 可以设置始终显示：
   - 设置 → 个性化 → 任务栏
   - 选择哪些图标显示在任务栏上

---

### Q5: 切换输入法失败？

**现象：** 弹窗提示"切换输入法失败"

**解决：**
1. 重启程序（右键托盘 → 退出 → 重新运行）
2. 检查系统输入法设置是否正常
3. 尝试以管理员身份运行

---

### Q6: 程序占用空间大？

**说明：**
- 程序文件：6.0 MB（包含了所有依赖）
- 临时文件：0.3 MB（自动释放到 %TEMP%）
- 总计：约 6.3 MB

**优势：** 单文件部署，无需额外依赖，值得！

---

## 🎨 特性说明

### ✅ 单文件部署
- 只需一个 exe 文件
- 无需安装
- 绿色软件

### ✅ 自动释放依赖
- 首次运行自动释放 im-select.exe 到临时目录
- 后续运行直接使用缓存
- 不影响性能

### ✅ 单例保护
- 防止重复启动
- 避免资源浪费
- 友好提示

### ✅ 系统托盘
- 后台运行
- 右键菜单
- 随时退出

### ✅ 系统弹窗
- 重要提示使用系统弹窗
- GUI 模式下也能看到
- 不依赖终端

---

## 📋 使用场景示例

### 场景1：日常办公

小王每天需要在 Word 中写中文文档，在代码编辑器中写英文代码。

**使用体验：**
- 打开 Word → Win+K → 中文输入法
- 打开 VSCode → Win+J → 英文输入法
- 无需鼠标，快速切换

---

### 场景2：Vim 用户

小李使用 Vim 编辑器，经常在插入模式和命令模式切换。

**使用体验：**
- 插入模式输入中文 → Win+K
- 返回命令模式 → Win+J 切换英文
- 避免命令被中文输入法干扰

---

### 场景3：U盘便携

小张经常在不同电脑工作，需要随身工具。

**使用体验：**
- U盘里放一个 `switch-input-method.exe`
- 插入任何电脑
- 双击运行，立即可用
- 不留痕迹

---

## 🎯 最佳实践

### 推荐设置

1. ✅ 添加到开机启动
2. ✅ 固定托盘图标显示
3. ✅ 以管理员身份运行（可选）

### 不推荐做法

1. ❌ 不要重复运行多个实例
2. ❌ 不要删除临时目录中的缓存文件（除非故障排除）
3. ❌ 不要将程序放在需要管理员权限的目录

---

## 📊 技术规格

| 项目      | 说明          |
| --------- | ------------- |
| 文件大小  | 6.0 MB        |
| 内存占用  | < 20 MB       |
| CPU 占用  | < 0.1%        |
| 启动时间  | < 500ms       |
| 支持系统  | Windows 10/11 |
| .NET 需求 | 无            |
| 其他依赖  | 无            |

---

## 🎊 总结

**v2.2 单文件版让使用更简单：**

1. **部署简单** - 只需一个文件
2. **使用方便** - 双击运行即可
3. **功能完整** - 托盘菜单、系统弹窗、单例保护
4. **性能优秀** - 低内存、低 CPU、快速启动

现在，享受这个专业的输入法切换工具吧！ 🚀

---

**程序名称**: 兴宜街道红旗路输入法切换工具  
**版本**: v2.2 (单文件版)  
**大小**: 6.0 MB  
**系统**: Windows 10/11  
**快捷键**: Win+J (英文) / Win+K (中文)
