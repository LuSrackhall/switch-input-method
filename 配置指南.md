# 按键绑定配置指南

## 概述

本程序支持自定义按键绑定和输入法切换。所有配置存储在 `config.json` 文件中。

## 配置文件位置

配置文件 `config.json` 位于程序可执行文件所在目录。

## 配置文件格式

```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "1033",
      "description": "切换到英文输入法"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "2052",
      "description": "切换到中文输入法"
    }
  ]
}
```

## 配置项说明

### key_bindings (数组)
包含所有按键绑定配置的列表。

#### 单个绑定配置项:

- **modifier_key** (数字): 修饰键的虚拟键码
  - 例如: 91 = 左 Win 键, 92 = 右 Win 键
  
- **function_key** (数字): 功能键的虚拟键码
  - 例如: 74 = J 键, 75 = K 键
  
- **im_key** (字符串): 输入法标识符
  - 通过运行 `im-select.exe` (不带参数) 可以获取当前输入法的标识符
  - 常见值:
    - "1033" - 英语(美国)
    - "2052" - 中文(中国)
  
- **description** (字符串): 该绑定的描述说明

## 常用虚拟键码参考

### 修饰键
- 91 (0x5B) - 左 Win 键
- 92 (0x5C) - 右 Win 键
- 160 (0xA0) - 左 Shift
- 161 (0xA1) - 右 Shift
- 162 (0xA2) - 左 Ctrl
- 163 (0xA3) - 右 Ctrl
- 164 (0xA4) - 左 Alt
- 165 (0xA5) - 右 Alt

### 字母键 (A-Z)
- 65-90 (0x41-0x5A)
- 例如: A=65, B=66, ..., J=74, K=75, ..., Z=90

### 数字键 (0-9)
- 48-57 (0x30-0x39)

### 功能键
- 112-123 (0x70-0x7B) - F1 到 F12

## 如何获取当前输入法的 im_key

1. 在命令行中运行程序目录下的 `im-select.exe` (不带参数)
2. 输出的字符串就是当前输入法的 im_key
3. 切换到你想要绑定的输入法,再次运行查看其 im_key

## 配置示例

### 示例 1: Win+J/K 切换中英文 (默认)
```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "1033",
      "description": "切换到英文输入法"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "2052",
      "description": "切换到中文输入法"
    }
  ]
}
```

### 示例 2: Ctrl+Shift+1/2 切换输入法
```json
{
  "key_bindings": [
    {
      "modifier_key": 162,
      "function_key": 49,
      "im_key": "1033",
      "description": "切换到英文"
    },
    {
      "modifier_key": 162,
      "function_key": 50,
      "im_key": "2052",
      "description": "切换到中文"
    }
  ]
}
```

### 示例 3: 多个输入法绑定
```json
{
  "key_bindings": [
    {
      "modifier_key": 91,
      "function_key": 74,
      "im_key": "1033",
      "description": "切换到英文"
    },
    {
      "modifier_key": 91,
      "function_key": 75,
      "im_key": "2052",
      "description": "切换到微软拼音"
    },
    {
      "modifier_key": 91,
      "function_key": 76,
      "im_key": "你的其他输入法key",
      "description": "切换到其他输入法"
    }
  ]
}
```

## 配置修改步骤

1. 右键点击系统托盘中的程序图标
2. 选择 "配置管理" -> "打开配置文件"
3. 在记事本中编辑 `config.json`
4. 保存文件
5. 右键托盘图标,选择 "配置管理" -> "重新加载配置"

或者:

1. 手动打开程序目录下的 `config.json` 文件
2. 按照上述格式编辑配置
3. 保存文件
4. 重启程序使配置生效

## 注意事项

1. **JSON 格式**: 配置文件必须是有效的 JSON 格式,注意:
   - 使用双引号,不要用单引号
   - 数字不要加引号
   - 最后一项后面不要有逗号
   
2. **修饰键**: 目前每个绑定只支持一个修饰键

3. **重复绑定**: 不要配置相同的按键组合,后面的会覆盖前面的

4. **生效方式**: 修改配置后需要重新加载或重启程序

## 托盘菜单功能

- **查看当前配置**: 显示当前的按键绑定
- **快速绑定当前输入法**: 查看如何将当前输入法绑定到快捷键(提供配置示例)
- **虚拟键码参考**: 查看常用按键的虚拟键码
- **打开配置文件**: 在记事本中打开配置文件
- **重新加载配置**: 应用最新的配置,无需重启程序

## 故障排除

### 配置文件损坏
如果配置文件损坏导致程序无法启动,只需删除 `config.json` 文件,程序会自动创建默认配置。

### 按键不生效
1. 检查配置文件格式是否正确
2. 确认已重新加载配置或重启程序
3. 检查虚拟键码是否正确
4. 确认 im_key 是否正确(可通过运行 im-select.exe 验证)

### 获取帮助
查看程序目录下的其他文档文件,或检查控制台输出的错误信息。
