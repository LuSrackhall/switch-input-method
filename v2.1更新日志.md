# 兴宜街道红旗路输入法切换工具 - 更新日志

## 🎉 v2.1 新功能 (2025-10-03)

### 1. 📛 正式命名

程序现在有了正式的名称：**兴宜街道红旗路输入法切换工具**

- ✅ 托盘图标标题更新
- ✅ 托盘提示信息更新  
- ✅ 启动界面更新

### 2. 🔒 全局单例模式

确保程序只能运行一个实例，避免资源浪费。

**实现原理:**
- 使用 Windows 全局互斥锁 (Global Mutex)
- 互斥锁名称: `Global\\XingYiStreetHongQiRoadIMESwitcher`
- 启动时自动检测是否已有实例运行

**用户体验:**
- 如果程序已在运行，再次启动会显示友好提示
- 不会创建重复的托盘图标
- 避免多个进程同时监听键盘造成冲突

**提示信息:**
```
⚠️  程序已在运行中!
请检查系统托盘图标
如需退出,请右键托盘图标选择退出
```

### 3. 📦 简化部署 - 使用同目录的 im-select.exe

不再需要硬编码路径！程序会自动查找 im-select.exe。

**查找顺序:**
1. 首先查找与 `switch-input-method.exe` 同目录下的 `im-select.exe`
2. 如果不存在，则尝试当前工作目录

**优势:**
- ✅ 便携式部署 - 只需将两个文件放在一起
- ✅ 无需配置路径
- ✅ 可以放在任意目录运行
- ✅ 适合打包分发

**部署结构:**
```
your-folder/
├── switch-input-method.exe    # 主程序
└── im-select.exe              # 输入法切换工具
```

## 🚀 使用方法

### 安装步骤

1. **准备文件**
   ```
   创建一个文件夹，放入两个文件：
   - switch-input-method.exe
   - im-select.exe
   ```

2. **直接运行**
   ```bash
   双击 switch-input-method.exe
   ```

3. **开机自启动** (可选)
   - 按 `Win + R`，输入 `shell:startup`
   - 将 `switch-input-method.exe` 的快捷方式放入该文件夹

### 日常使用

- **启动**: 双击程序，托盘图标出现
- **切换输入法**: 
  - `Win + J` → 英文
  - `Win + K` → 中文
- **退出**: 右键托盘图标 → 选择"退出"

### 重复启动保护

如果不小心再次启动程序：
- ✅ 会显示友好提示
- ✅ 不会创建重复进程
- ✅ 原有实例继续正常工作

## 🔧 技术实现细节

### 1. 全局单例实现

```go
func ensureSingleInstance() bool {
    // 创建全局命名互斥锁
    mutexName := "Global\\XingYiStreetHongQiRoadIMESwitcher"
    
    // 使用 Windows API CreateMutex
    handle, _, err := procCreateMutex.Call(...)
    
    // 检查错误码 ERROR_ALREADY_EXISTS (183)
    if errno == 183 {
        return false // 已有实例在运行
    }
    
    return true // 可以继续运行
}
```

### 2. 智能路径查找

```go
func getIMSelectPath() string {
    // 获取当前执行文件路径
    exePath, _ := os.Executable()
    exeDir := filepath.Dir(exePath)
    
    // 构建同目录路径
    imSelectPath := filepath.Join(exeDir, "im-select.exe")
    
    // 检查文件是否存在
    if _, err := os.Stat(imSelectPath); err == nil {
        return imSelectPath // 找到了，使用完整路径
    }
    
    // 降级到相对路径
    return "im-select.exe"
}
```

### 3. 托盘名称更新

```go
func onReady() {
    systray.SetTitle("兴宜街道红旗路输入法切换工具")
    systray.SetTooltip("兴宜街道红旗路输入法切换工具\nWin+J: 英文\nWin+K: 中文")
    // ...
}
```

## 📊 功能对比

| 功能         | v2.0 | v2.1 (新版) |
| ------------ | ---- | ----------- |
| 托盘运行     | ✅    | ✅           |
| 隐藏窗口切换 | ✅    | ✅           |
| 单例模式     | ❌    | ✅           |
| 智能路径查找 | ❌    | ✅           |
| 正式命名     | ❌    | ✅           |
| 硬编码路径   | ✅    | ❌           |

## 🎯 最佳实践

### 推荐部署方式

**方式一: 单一文件夹**
```
C:\Programs\IMSwitcher\
├── switch-input-method.exe
└── im-select.exe
```

**方式二: 用户目录**
```
C:\Users\YourName\Tools\
├── switch-input-method.exe
└── im-select.exe
```

**方式三: 便携式 (U盘)**
```
E:\PortableApps\IMSwitcher\
├── switch-input-method.exe
└── im-select.exe
```

### 注意事项

1. **两个文件必须在同一目录**
   - `switch-input-method.exe`
   - `im-select.exe`

2. **避免重复启动**
   - 程序已有单例保护
   - 重复启动会自动退出并提示

3. **管理员权限** (某些情况需要)
   - 如果在某些应用中快捷键不生效
   - 尝试以管理员身份运行

## 🐛 故障排除

### 问题: 提示"切换输入法失败"

**原因**: 找不到 im-select.exe

**解决方案**:
1. 确认 `im-select.exe` 与主程序在同一目录
2. 检查文件权限
3. 尝试以管理员身份运行

### 问题: 提示"程序已在运行中"

**原因**: 已有实例在运行

**解决方案**:
1. 检查系统托盘图标
2. 如需重启，先右键托盘图标退出
3. 然后重新启动

### 问题: 找不到托盘图标

**原因**: 托盘图标可能被隐藏

**解决方案**:
1. 点击任务栏右下角的 `^` (展开隐藏图标)
2. 在 Windows 设置中调整托盘图标显示
3. 设置 → 个性化 → 任务栏 → 选择哪些图标显示在任务栏上

## 📝 版本历史

### v2.1 (2025-10-03)
- ✅ 添加正式名称 "兴宜街道红旗路输入法切换工具"
- ✅ 实现全局单例模式
- ✅ 支持智能路径查找 im-select.exe
- ✅ 简化部署流程

### v2.0 (2025-10-03)
- ✅ 添加系统托盘功能
- ✅ 修复终端窗口闪现问题
- ✅ 支持后台运行

### v1.0
- ✅ 基础键盘钩子功能
- ✅ Win+J/K 快捷键切换

## 🎊 总结

v2.1 版本带来了三个重要改进：

1. **品牌化** - 有了正式的名称
2. **智能化** - 自动查找依赖文件
3. **稳定化** - 单例模式避免重复启动

现在程序更专业、更易用、更稳定！

---

**项目名称**: 兴宜街道红旗路输入法切换工具  
**版本**: v2.1  
**日期**: 2025-10-03  
**状态**: ✅ 稳定版
